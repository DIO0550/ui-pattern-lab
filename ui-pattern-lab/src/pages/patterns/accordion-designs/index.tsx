import type {ReactNode} from 'react';
import AccordionPatternPageContent from '@site/src/components/AccordionPatternPageContent';

/** Renders the accordion pattern comparison page. */
export default function AccordionDesignsPage(): ReactNode {
  return <AccordionPatternPageContent />;
}
